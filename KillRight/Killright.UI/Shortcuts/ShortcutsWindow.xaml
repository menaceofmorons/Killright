<Window x:Class="Killright.UI.Shortcuts.ShortcutsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="KillRight — Shortcuts"
        Width="360"
        Height="240"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <DockPanel Margin="12">
        <Button DockPanel.Dock="Bottom"
                Content="Close"
                Width="80"
                HorizontalAlignment="Right"
                Margin="0,12,0,0"
                Click="Close_Click" />
        <ListBox BorderThickness="0"
                 Background="Transparent">
            <ListBoxItem Content="Ctrl+M — Open menu" />
            <ListBoxItem Content="Ctrl+Q — Quit" />
            <ListBoxItem Content="F1 — Show this shortcut list" />
            <ListBoxItem Content="Alt + drag — Move window" />
            <ListBoxItem Content="Win+Down — Minimize window" />
        </ListBox>
    </DockPanel>
</Window>
