<Window x:Class="Killright.UI.DeveloperTools.GroupDetectionHistoryPilot.GroupDetectionHistoryPilotWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Group Detection One-Day History Import"
        Height="640"
        Width="940"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Developer-only one-day history importer. Uses the existing working zKill history download, parse and qualification logic. Persists only day-level status counts. Does not write evidence, participant or relationship summary rows."
                   TextWrapping="Wrap"
                   Margin="0,0,0,12" />

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    Margin="0,0,0,12">
            <TextBlock Text="UTC date:"
                       VerticalAlignment="Center"
                       Margin="0,0,6,0" />

            <TextBox x:Name="ImportDateTextBox"
                     Width="120"
                     Margin="0,0,12,0" />

            <Button x:Name="StartButton"
                    Content="Import One Day Counts"
                    Width="170"
                    Height="32"
                    Margin="0,0,8,0"
                    Click="StartButton_Click" />

            <Button x:Name="CopyResultsButton"
                    Content="Copy Results"
                    Width="120"
                    Height="32"
                    IsEnabled="False"
                    Click="CopyResultsButton_Click" />
        </StackPanel>

        <TextBox Grid.Row="2"
                 x:Name="ResultTextBox"
                 Text="Enter a UTC date or use the default, then click Import One Day Counts."
                 IsReadOnly="True"
                 AcceptsReturn="True"
                 AcceptsTab="True"
                 TextWrapping="NoWrap"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas"
                 FontSize="12" />
    </Grid>
</Window>