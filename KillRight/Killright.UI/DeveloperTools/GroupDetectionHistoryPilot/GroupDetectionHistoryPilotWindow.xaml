<Window x:Class="Killright.UI.DeveloperTools.GroupDetectionHistoryPilot.GroupDetectionHistoryPilotWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Group Detection History Pilot"
        Height="260"
        Width="520"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Counts zKill history rows for the previous complete UTC calendar month. No data is stored."
                   TextWrapping="Wrap"
                   Margin="0,0,0,12" />

        <Button Grid.Row="1"
                x:Name="StartButton"
                Content="Start"
                Width="120"
                Height="32"
                HorizontalAlignment="Left"
                Click="StartButton_Click" />

        <TextBlock Grid.Row="2"
                   x:Name="ResultTextBlock"
                   Text="Click Start to begin."
                   TextWrapping="Wrap"
                   Margin="0,16,0,0" />
    </Grid>
</Window>