<Window x:Class="Killright.UI.DeveloperTools.GroupDetectionHistoryPilot.GroupDetectionHistoryPilotWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Group Detection Winter Nexus Quarter Pilot"
        Height="620"
        Width="900"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Counts zKill R2 history volume, qualification filters, candidate storage row counts, and relationship frequency distribution for 2025-11-01 to 2026-01-31. No data is stored."
                   TextWrapping="Wrap"
                   Margin="0,0,0,12" />

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    Margin="0,0,0,12">
            <Button x:Name="StartButton"
                    Content="Start"
                    Width="120"
                    Height="32"
                    Margin="0,0,8,0"
                    Click="StartButton_Click" />

            <Button x:Name="CopyResultsButton"
                    Content="Copy Results"
                    Width="120"
                    Height="32"
                    IsEnabled="False"
                    Click="CopyResultsButton_Click" />
        </StackPanel>

        <TextBox Grid.Row="2"
                 x:Name="ResultTextBox"
                 Text="Click Start to begin."
                 IsReadOnly="True"
                 AcceptsReturn="True"
                 AcceptsTab="True"
                 TextWrapping="NoWrap"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas"
                 FontSize="12" />
    </Grid>
</Window>